<Window x:Class="Zen_Music.SignUpPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sign Up"
        Width="660" Height="470"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <SolidColorBrush x:Key="BgDark"      Color="#1A1D2E"/>
        <SolidColorBrush x:Key="BgPanel"     Color="#1E2236"/>
        <SolidColorBrush x:Key="AccentBlue"  Color="#8EBBFF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#EAEAF5"/>
        <SolidColorBrush x:Key="TextMuted"   Color="#7B80A8"/>
        <SolidColorBrush x:Key="InputBg"     Color="#DDDCDC"/>

        <Style x:Key="ZenInput" TargetType="TextBox">
            <Setter Property="Background"             Value="{StaticResource InputBg}"/>
            <Setter Property="Foreground"             Value="#1A1D2E"/>
            <Setter Property="BorderThickness"        Value="0"/>
            <Setter Property="FontSize"               Value="13"/>
            <Setter Property="Height"                 Value="30"/>
            <Setter Property="Padding"                Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ZenPassword" TargetType="PasswordBox">
            <Setter Property="Background"             Value="{StaticResource InputBg}"/>
            <Setter Property="Foreground"             Value="#1A1D2E"/>
            <Setter Property="BorderThickness"        Value="0"/>
            <Setter Property="FontSize"               Value="13"/>
            <Setter Property="Height"                 Value="30"/>
            <Setter Property="Padding"                Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SignUpBtn" TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground"      Value="#FFF"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="14" Padding="20,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#82BEFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" ClipToBounds="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Лява страна - синя илюстрация -->
            <Border Grid.Column="0" CornerRadius="12,0,0,12">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#2A3A6E" Offset="0"/>
                        <GradientStop Color="#1A2550" Offset="0.5"/>
                        <GradientStop Color="#8BAFD4" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <!-- Вълни -->
                    <Canvas>
                        <Path Canvas.Left="-40" Canvas.Top="20"
                              Fill="#3A5A9A" Opacity="0.5">
                            <Path.Data>
                                <PathGeometry>
                                    <PathFigure StartPoint="0,300">
                                        <BezierSegment Point1="80,200" Point2="160,350" Point3="340,250"/>
                                        <LineSegment Point="340,400"/>
                                        <LineSegment Point="0,400"/>
                                    </PathFigure>
                                </PathGeometry>
                            </Path.Data>
                        </Path>
                        <Path Canvas.Left="-30" Canvas.Top="35"
                              Fill="#5A80C0" Opacity="0.4" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <Path.Data>
                                <PathGeometry>
                                    <PathFigure StartPoint="0,320">
                                        <BezierSegment Point1="100,220" Point2="200,370" Point3="380,260"/>
                                        <LineSegment Point="380,400"/>
                                        <LineSegment Point="0,400"/>
                                    </PathFigure>
                                </PathGeometry>
                            </Path.Data>
                        </Path>
                        <Path Canvas.Left="-40" Canvas.Top="70"
                              Fill="#7AAAD8" Opacity="0.35" HorizontalAlignment="Center" VerticalAlignment="Top">
                            <Path.Data>
                                <PathGeometry>
                                    <PathFigure StartPoint="0,340">
                                        <BezierSegment Point1="120,240" Point2="220,390" Point3="400,280"/>
                                        <LineSegment Point="400,400"/>
                                        <LineSegment Point="0,400"/>
                                    </PathFigure>
                                </PathGeometry>
                            </Path.Data>
                        </Path>
                    </Canvas>
                    <!-- Текст -->
                    <StackPanel VerticalAlignment="Center" Margin="36,0,20,60">
                        <TextBlock VerticalAlignment="Center">
                            <Run Text="Welcome to " Foreground="White"
                                 FontSize="22" FontWeight="Bold"/>
                            <Run Text="ZEN" Foreground="{StaticResource AccentBlue}"
                                 FontSize="22" FontWeight="Bold"/>
                        </TextBlock>
                        <Border Width="180" Height="1" HorizontalAlignment="Left"
                                Margin="0,8,0,0" Background="#4A6A9A"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Дясна страна -->
            <Border Grid.Column="1" Background="{StaticResource BgPanel}" CornerRadius="0,12,12,0">
                <Grid>
                    <!-- Settings бутон -->
                    <Button HorizontalAlignment="Right" VerticalAlignment="Top"
                            Margin="0,10,10,0" Style="{StaticResource SignUpBtn}"
                            Background="Transparent" Click="btnSettings_Click">
                        <TextBlock Text="⚙" FontSize="16" Foreground="{StaticResource TextMuted}"/>
                    </Button>

                    <StackPanel VerticalAlignment="Center" Margin="40,0,40,0">
                        <TextBlock Text="Sign Up" Foreground="{StaticResource TextPrimary}"
                                   FontSize="20" FontWeight="Bold"
                                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

                        <!-- Username -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Username"
                                       Foreground="{StaticResource TextPrimary}"
                                       FontSize="13" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" x:Name="txtUsername"
                                     Style="{StaticResource ZenInput}"/>
                        </Grid>

                        <!-- Email -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="E-Mail"
                                       Foreground="{StaticResource TextPrimary}"
                                       FontSize="13" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" x:Name="txtEmail"
                                     Style="{StaticResource ZenInput}"/>
                        </Grid>

                        <!-- Password -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Password"
                                       Foreground="{StaticResource TextPrimary}"
                                       FontSize="13" VerticalAlignment="Center"/>
                            <PasswordBox Grid.Column="1" x:Name="txtPassword"
                                         Style="{StaticResource ZenPassword}"/>
                        </Grid>

                        <!-- Already a member + Sign Up -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Background="Transparent"
                                    BorderThickness="0" Cursor="Hand"
                                    HorizontalAlignment="Left"
                                    Click="btnAlreadyMember_Click">
                                <TextBlock Foreground="{StaticResource TextMuted}"
                                           FontSize="11" TextWrapping="Wrap"
                                           Text="Already a&#x0a;member?"/>
                            </Button>
                            <Button Grid.Column="1" Content="Sign Up"
                                    Style="{StaticResource SignUpBtn}"
                                    Click="btnSignUp_Click"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>